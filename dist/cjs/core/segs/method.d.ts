import type { PathSeg, ResolveResult } from '../types.js';
export declare function resolveMethod(node: any, seg: PathSeg, src: string, prevAsName: string | null): ResolveResult;
export declare function resolveMethodAt(node: any, seg: PathSeg, src: string, prevAsName: string | null): ResolveResult;
